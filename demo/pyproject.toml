[project]
name = "hooke-schema-demo"
version = "0.1.0"
description = "Schema-governed pipeline demo"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2",
    "numpy>=1.26",
    "weave>=0.51.0",
    "hydra-core>=1.3.0",
    "hydra-zen>=0.13.0",
]

[dependency-groups]
dev = ["pytest>=8"]
