[project]
name = "hooke-schema-demo"
version = "0.1.0"
description = "Schema-governed pipeline demo"
requires-python = ">=3.11"
dependencies = ["pydantic>=2"]

[dependency-groups]
dev = ["pytest>=8"]
